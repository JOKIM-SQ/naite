# S06 스파이크 — 서버리스 래핑

- 목표: 브라우저가 아닌 서버 함수에서 Claude API를 호출하고 구조화 JSON을 받을 수 있는지 확인
- 현재 결과: 요청 구성·응답 JSON 정규화는 자동 테스트로 확인됨
- 실제 API 호출: `ANTHROPIC_API_KEY`가 로컬·프로젝트 환경에 없어 미실행
- 다음 검증: Vercel 환경변수 설정 후 리뷰 10개로 `/api/analyze`를 호출하고, 브라우저 개발자도구에서 키가 보이지 않는지 확인
