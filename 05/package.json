{
  "name": "s05-spigen-case-catalog",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test",
    "lint": "node --check api/catalog-meta.mjs && node --check api/catalog.mjs && node --check api/config.mjs && node --check public/app.js",
    "build": "npm run lint"
  },
  "dependencies": {
    "cheerio": "1.1.2"
  }
}
