# S07 검증 기록 — 2026-09-16

## 판정

구현·로컬 검증 완료. 실제 Supabase·Claude 연동과 영수증 인식률 검증은 외부 키/SQL 적용 대기. 제품 전체 완료 또는 제출 완료로 표시하지 않는다.

## 자동 검사

- `npx tsc --noEmit`: 종료 0. JS emit 없는 컴파일 확인(checkJs=false); 엄격한 타입 검사는 아님.
- `npm run lint`: 모든 JS 구문 검사 통과.
- `npm test`: 35/35 통과 (백엔드19, 프런트15, 개발서버1).
- `npm run build`: 종료 0.
- 테스트 먼저 실패 확인 후 구현; backend-tdd.md/frontend-tdd.md/runtime-tdd.md 참조.

## 실제 브라우저 관찰

`/browse`의 `$B` 사용. 포트3071/3072는 **테스트 전용 외부 응답 대역**이며 실제 제품 API·검증·Storage 어댑터·세션·추출 parser·UI 코드를 실행한다. 이 서버를 프로덕션에 배포하지 않는다. 아래 관찰은 UI/계약 검증이며 외부 서비스 성공이나 OCR 정확도의 증거가 아니다.

| 시나리오 | 관찰 |
|---|---|
| 사진 3장 선택 | 파일3개 업로드 후 ready3개, 날짜·금액 요약표와 원본 이미지3개 표시 |
| 자동 저장 | 총액14.5→9 수정, 저장 버튼 없이 서버값9·수정횟수1. 최초값14.5 유지 |
| 새로고침 | 같은 브라우저에서3개 복원, 수정값9 유지 |
| 일부 분석 실패 | 실패1개와 기존ready3개 공존; 원본과 단건재시도 버튼 표시 |
| 단건 재시도 | 외부 오류해제 후 해당 버튼 클릭; 동일행을 ready로 복구 |
| 파일 수 초과 | 4장 선택 시 '한 번에 사진을 3장까지' 안내, 추가행 없음 |
| 날짜 오류 | 2025-02-29 입력 차단, aria-invalid와 오류문구 표시 |
| 품목 편집 | 새 품목명·수량1·금액2.5 저장, original.items는 불변. 품목삭제 동작 |
| 세션 격리 | 다른 호스트 쿠키 세션 localhost:3071의 목록0; 기존127.0.0.1 세션은 목록 유지. 단위/HTTP 검사에서도 타세션 PATCH/retry404 |
| HttpOnly | document.cookie에서 s07_session 읽을 수 없음 |
| 느린409+입력중 | 서버의 원격품목추가 후 이 탭 total변경. 1200ms 조회지연 중 타이핑한 품목명·focus 보존. blur 뒤 total22 및 품목명 저장, original 유지 |
| 모바일 | 390×844에서 scrollWidth=390. 요약표와 편집 카드 가로넘침 없음 |
| 누락 키 | 실제 제품서버3070 GET503, '서버 저장소 연결 설정이 필요합니다' 표시, 업로드 disabled |
| 콘솔 | 성공 흐름 예외 없음. 의도한409와 미설정 서버503 네트워크 실패는 원인과 함께 구분 |

스크린샷: fixture-desktop.png, fixture-mobile.png, fixture-summary-mobile.png. 이미지 자체는 공개 SROIE 샘플이고 화면의 추출값은 테스트 fixture임을 주의.

## 독립 에이전트 검증

s07_verify가 API/SQL/화면 계약을 읽기전용 검토하고 실제 app.js 최소DOM 실행으로6개 경계를 검증했다. 분석 중 재진입 polling 누락 및 409 품목 구조변경/원시 입력 소실을 찾아 수정 후 해소 확인. 최종 잔여 P1/P2 없음. 엄격한 실서비스 검증 통과를 의미하지 않는다.

## 실제 OCR 평가 준비

출처: https://github.com/zzzDavid/ICDAR-2019-SROIE (data/img 및 data/key). 공개 샘플은 임시 폴더에만 받았으며 원본3개를 앱에 하드코딩하거나 배포하지 않는다.

| 사진 | 공개 정답 날짜 | 공개 정답 총액 | Haiku 최초 결과 |
|---|---|---|---|
| 000.jpg | 25/12/2018 | 9.00 | 미실행 |
| 001.jpg | 19/10/2018 | 60.30 | 미실행 |
| 002.jpg | 12-01-19 | 33.90 | 미실행; 연/월/일 순서의 모호함 검토 필요 |

최종 평가에는 사진 원본과 정답을 함께 확인하고, 최초 추출의 날짜·최종금액이 둘 다 맞은 수 N/3을 기록한다. UI의 correctionCount는 최상위필드 변경 수(items전체1)라 실제 사용자 작업 수와 다를 수 있다. 업로드 후 작업3회 이하와 3분 시연은 실제서비스에서 별도 관찰해야 한다.

## 남은 외부 단계

- 사용자: 07/.env.local 또는 S07 Vercel 프로젝트에 실제 Supabase/Claude 키 설정. 대화/Git에 비밀값 금지.
- 사용자 또는 권한 있는 연결: 기존 Supabase kmfoeoxvsadlurpmkqwh에 07/SUPABASE.sql 적용 및 weekly_projects 노출 확인. MCP는 다른 프로젝트여서 사용하지 않았다.
- 키 준비 후 원본저장·실제Haiku·재조회·수정·OCR N/3 및 비용/시간 측정.
- 그 결과로 SPIKE.md와 report.html 완성, 갤러리 드랍/스택 비교표 제출 확인.
